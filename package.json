{
  "name": "@ideaspaces/pi-agent-sessions",
  "version": "0.2.0",
  "description": "Persistent, bounded fellow-agent sessions for Pi",
  "type": "module",
  "license": "MIT",
  "author": "IdeaSpaces",
  "homepage": "https://github.com/IdeaSpaces-xyz/pi-agent-sessions#readme",
  "bugs": {
    "url": "https://github.com/IdeaSpaces-xyz/pi-agent-sessions/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IdeaSpaces-xyz/pi-agent-sessions.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "pi-package",
    "pi",
    "agent",
    "rpc",
    "multi-agent"
  ],
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "check:package": "npm run build && node scripts/check-package.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:real": "vitest run test/real-smoke.test.ts test/real-turn-smoke.test.ts test/real-parent-smoke.test.ts test/real-conversation-smoke.test.ts",
    "prepare": "npm run build",
    "pack:check": "npm pack --dry-run"
  },
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.85.1",
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@earendil-works/pi-tui": "^0.85.1",
    "@types/node": "^25.5.2",
    "typebox": "^1.3.7",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
